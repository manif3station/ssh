# DD-033

## Title

Implement shared ssh-agent state across sessions.

## Method

TDD

## Acceptance

- missing or dead `SSH_AUTH_SOCK` starts or reuses a managed agent
- the managed agent uses a stable socket path so new DD prompt sessions and collectors can find the same agent
- the skill writes a shell-readable env file for shells that choose to source it
- `~/.ssh/config` includes the skill-managed `IdentityAgent` include without destroying user config

## Status

Done
