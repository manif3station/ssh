# EPIC-011

## Title

List managed SSH keys.

## Outcome

Add read-only commands that show which keys the `ssh` skill manages and whether each key is currently loaded in ssh-agent.

## Tickets

- `DD-038` Add `ssh.list` and `ssh.ls`
