# DD-043

## Title

Move the ssh key registry to `~/.ssh/keys.txt`.

## Method

TDD

## Acceptance

- `dashboard ssh.add` stores remembered keys in `~/.ssh/keys.txt`
- `dashboard ssh.list` and collector mode read that same registry
- a legacy `config/ssh/keys.txt` file is migrated to `~/.ssh/keys.txt` when present
- Docker tests pass
- `lib/SSH/Add.pm` reaches `100.0%` statement coverage and `100.0%` subroutine coverage
- docs, changelog, commit, and push are completed

## Status

Done
