# DD-036

## Title

Move managed ssh-agent files under `~/.ssh/ssh-agent`.

## Method

TDD

## Problem

The initial `ssh` skill stored managed ssh-agent runtime files under `~/.developer-dashboard/ssh-agent`. Some users may track their DD root with git, so volatile socket/env files should not be created there.

## Acceptance

- managed agent socket uses `~/.ssh/ssh-agent/agent.sock`
- managed env file uses `~/.ssh/ssh-agent/agent.env`
- docs no longer direct users to `~/.developer-dashboard/ssh-agent`
- Docker tests and coverage pass
- latest DD source proof passes
- release commit and push complete

## Status

Done
