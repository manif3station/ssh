# EPIC-016

## Title

Persist the ssh managed key registry under `~/.ssh`.

## Goal

Keep remembered SSH keys outside the installed skill folder so skill updates do not remove the managed key registry.

## Tickets

- `DD-043` Move the ssh key registry to `~/.ssh/keys.txt`.
