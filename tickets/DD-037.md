# DD-037

## Title

Fix shell agent bridge and missing-key UX.

## Method

TDD

## Problem

Real installed use showed three gaps: stale `SSH_AUTH_SOCK` checks leaked raw `ssh-add -l` socket errors, explicit missing keys produced confusing `ssh-add` failures after registration, and the managed `~/.ssh/ssh-agent/agent.env` file was written but not sourced by new shells.

## Acceptance

- stale `SSH_AUTH_SOCK` checks use quiet captured health checks
- explicit missing keys fail before registry writes or `ssh-add`
- explicit missing keys remove matching stale registry entries left by older failed runs
- expected user-facing failures do not include Perl source file suffixes
- successful add output includes registry, shell env, and source command fields
- shell startup files source `~/.ssh/ssh-agent/agent.env` for future shells
- Docker tests and coverage pass
- installed DD command proof passes
- latest DD source command proof passes
- release commit and push complete

## Status

Done
