# DD-031

## Title

Create the `ssh` skill records and baseline layout.

## Method

BDD

## Acceptance

- `skills/ssh` is an isolated git repository
- the skill has `.env`, `Changes`, `README.md`, `docs/`, `tickets/`, and `t/`
- the skill declares the `door-opener` collector in `config/config.json`

## Status

Done
